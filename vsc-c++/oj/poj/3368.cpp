#include <bits/stdc++.h>
using namespace std;
#define ll long long
typedef pair<int, int> Pii;
typedef pair<ll, ll> Pll;
const int N = 0;

signed main()
{
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    
    return 0;
}